This directory has the codes corresponding to the `backend` part.

Make sure you get into _Virtual environment_: `venv\Scripts\activate`

- You'll see `(venv) ..... ` in the terminal's prompt, if its present, then you're currently working in the virtual environment
